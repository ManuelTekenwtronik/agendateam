# Local Netlify folder
.netlify

# Dependencies
node_modules/

# Deno / Edge functions lockfile (generado por netlify dev)
deno.lock

# Env
.env
.env.*

# Backups de la base (respaldos locales, no subir)
backups/
